(define-module (sdl2 config)
  #:export (%libsdl2
            %libsdl2-image
            %libsdl2-ttf
            %libsdl2-mixer))

(define %libsdl2
  "libSDL2")

(define %libsdl2-image
  "libSDL2_image")

(define %libsdl2-ttf
  "libSDL2_ttf")

(define %libsdl2-mixer
  "libSDL2_mixer")
